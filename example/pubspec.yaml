name: example
description: An example application for the package.

environment:
  sdk: '>=3.7.0 <4.0.0'

dependencies:
  grpc: ^5.0.0
  protobuf: ^6.0.0

dev_dependencies:
  build_runner: ^2.4.0
  lints: ^6.0.0
  protoc_builder:
    path: ../

